# Place your TMDB 5000 dataset CSV files here:
# - tmdb_5000_movies.csv   (5.4 MB)
# - tmdb_5000_credits.csv  (38.2 MB)
#
# Download from:
# https://www.kaggle.com/datasets/tmdb/tmdb-movie-metadata
#
# These files are NOT committed to GitHub (see .gitignore)
# because they are large binary files.
# Each developer must download them separately.
#
# After downloading, run:
#   python preprocess.py   → verify data loads correctly
#   python model.py        → build the similarity matrix
